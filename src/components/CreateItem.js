import React from 'react';
import styled from 'styled-components';

const CreateItem = () => {
	return <Container>Create Item</Container>;
};

export default CreateItem;

const Container = styled.div`
	/* height: 100vh;
	width: 50vw;
	position: absolute;
    top: 0;
    right: 0;
	z-index: 20;
	background-color: hsl(0, 0%, 100%); */
`;
